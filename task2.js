// Как проверить, что строка является палиндромом?
// Палиндром — это слово, фраза или другой тип строки, который читается одинаково слева направо и наоборот. 

const strArr = ['Анна', 'Потоп']
const numArr = [808, 880]

const palindrome = arr => {

}

console.log(palindrome(strArr));
console.log(palindrome(numArr));