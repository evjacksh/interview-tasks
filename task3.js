// Нужно отсортировать массив в двух направлениях по возрастанию и по убыванию

const nums = [5, 23, 16, 10]

const order = {
    DESC: 'desc',
    ASC: 'asc'
}

const sort = (arr, orderParam) => {

} 

console.log(sort(nums, order.ASC))
console.log(sort(nums, order.DESC))